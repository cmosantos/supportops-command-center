# ST-08 Validation Evidence

Date: 2026-08-02

Environment: Windows, Python 3.12, Docker Desktop 29.5.3, Compose 5.1.4,
container Python 3.12.11 slim, deterministic offline application runtime

## Checkpoint and scope

- Started clean on `main` from ST-06 commit `71f93d165fff5d5af41530cff581050d1240ff2a`.
- Baseline `0.6.0`; final version `1.0.0`; Increment 7/Ollama remained excluded.
- No license, remote, push, PR, tag, release, registry publication, credential,
  real data, or external service was used.

## Commands and results

| Command or gate | Result | Exit |
|---|---|---:|
| `.\\.venv\\Scripts\\python.exe -m ruff check .` | PASS | 0 |
| `.\\.venv\\Scripts\\python.exe -m mypy src tests` | PASS, 63 files | 0 |
| `.\\.venv\\Scripts\\python.exe -m pytest -q` | 256 passed, 3 skipped | 0 |
| `uv build --offline` | wheel and sdist 1.0.0 | 0 |
| wheel archive inventory | 40 files, five runbooks, no tests/runtime data | 0 |
| clean wheel install and CLI smoke | version, DB init/status, search PASS | 0 |
| installed Streamlit smoke | health HTTP 200; root HTTP 200 | 0 |
| `docker compose config` / `build` / `up -d` | valid, built, healthy | 0 |
| container identity and SQLite checks | UID/GID 10001; 4 migrations; integrity `ok`; FK 0 | 0 |
| restart and `--force-recreate` | incident and two exports persisted | 0 |
| `docker compose down` without `-v` | stopped; review volumes retained | 0 |
| `git diff --check` | PASS | 0 |
| `git remote -v` | empty | 0 |

The three skips are retained environment-conditioned Windows symlink/reparse tests.
Containment is exercised where creation is supported, and production code rejects
resolved-path escapes and existing reparse components.

## Delivery evidence

- A fresh runtime initialized an empty SQLite database idempotently, applied four
  checksummed migrations, and returned integrity `ok` with no foreign-key violations.
- The installed wheel loaded from `site-packages`, reported `1.0.0`, ran CLI flows,
  and launched Streamlit independently of source-tree imports.
- The 40-file wheel includes five runbooks and excludes tests, `.env`, caches,
  databases, exports, screenshots, logs, virtual environments, and Git metadata.
- The complete synthetic demo covers lifecycle, triage, knowledge search, performed
  procedures, nine-section documentation, Markdown/JSON exports, and dashboard.
- Docker derives the runtime environment from `uv.lock`, installs the project wheel
  with `--no-deps`, runs non-root with read-only root, drops all capabilities, and
  mounts separate database/export volumes. One incident and both exports survived
  restart and forced recreation.

## Security, reviews, and limitations

- Adversarial tests cover SQL-like payloads, traversal/symlink escape, hostile
  Markdown/HTML/Unicode/control input, cross-incident isolation, deleted records,
  duplicate/rerun behavior, corrupt inputs, safe errors, rollback, and prohibited APIs.
- Local scans found no project secret, credential, PII, absolute developer path,
  forbidden tracked artifact, application shell/admin execution, unsafe dynamic
  evaluation/deserialization, LLM, embedding, vector database, or product network path.
- CI uses least permissions and has no deploy, publish, registry, or secret step.
- QA and DevOps initially identified unlocked image dependencies, incomplete demo,
  screenshot reparse containment, and missing administration. All Medium findings
  were corrected and independently re-reviewed.
- Residual Low items: base image uses a tag rather than digest; exact builder tools
  lack hashes; no online vulnerability database audit was attempted offline.
  Product and publication limitations remain in `docs/known-limitations.md` and
  `docs/pending.md`.
