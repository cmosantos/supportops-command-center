# ST-06 Validation Evidence

Date: 2026-08-02

Environment: Windows, Python 3.12, Streamlit 1.60.0, offline application runtime

## Checkpoint and scope

- Started clean on `main` from ST-05 commit
  `ccc22a1cb1b077f4c852524ba0332085d8b62153`.
- Baseline version `0.5.0`; 218 passed, 2 conditional symlink skips.
- Increment 6 only: no Docker/GitHub hardening and no Ollama/Increment 7.

## Functional evidence

- One shared composition root constructs a typed Application façade used by
  Streamlit; CLI behavior remains canonical and unchanged.
- UI flows cover database bootstrap/status, incident create/filter/detail/full
  update/close/reopen/confirmed soft delete/history, full JSON triage/history,
  knowledge ranking/evidence, performed history, nine-section documentation,
  both downloads, and observational dashboard.
- Migration 4 persists explicit category; category is never inferred from rule ID.
- Session state binds triage/document results to incident ID. Behavioral tests
  prove incident switching and reruns do not leak or duplicate mutations.
- Dashboard SQLite tests prove P2/P4 groups, sorted categories, escalation count,
  open/closed counts, latest-close duration of 50 seconds, stable snapshots, and
  soft-delete exclusion.

## Thin-adapter and security evidence

- Static boundary tests reject direct SQLite, persistence, filesystem, runbook,
  concrete exporter/search/policy, unsafe HTML, shell, network, LLM, and dynamic
  execution access in Streamlit.
- User/persisted text is rendered through safe Streamlit APIs; errors omit SQL,
  paths, tracebacks, secrets, configuration, and payloads.
- Delete requires explicit confirmation and reason; cancel/rerun is inert.
- Suggested and performed procedures remain visibly separate and non-executable.

## Commands and results

| Command | Result | Exit |
|---|---|---:|
| `.\\.venv\\Scripts\\python.exe -m ruff check .` | PASS | 0 |
| `.\\.venv\\Scripts\\python.exe -m mypy src tests` | PASS, 60 files | 0 |
| `.\\.venv\\Scripts\\python.exe -m pytest -q` | 245 passed, 2 skipped | 0 |
| presentation/facade/smoke focused suites | 27 passed | 0 |
| `uv build --offline` | wheel and sdist 0.6.0 | 0 |
| `git diff --check` | PASS | 0 |

## Runtime smokes

- Automated source-tree Streamlit smoke reserves a dynamic loopback port, uses an
  absolute app path, checks health and page root HTTP 200, validates safe logs,
  and guarantees termination.
- A fresh project-local `.venv-smoke` installed the 0.6.0 wheel plus 50 resolved
  dependencies. Installed CLI returned `0.6.0`; the app module loaded from
  `site-packages`; installed Streamlit health and root returned HTTP 200 on a
  dynamic loopback port; the process stopped and logs contained no traceback or
  credential markers.

The two skips are environment-conditioned Windows symlink creation checks retained
from ST-04/ST-05. They do not affect Streamlit functional coverage.

## Independent QA

QA initially raised broken knowledge rendering, incomplete behavioral coverage,
cross-incident session leakage, incorrect category semantics, incomplete filters/
updates/triage, dashboard gaps, and weak runtime smoke. All findings were fixed
and re-reviewed. Final verdict: PASS, zero open Critical, High, or Medium findings.
AC1-AC28 passed technically; AC29-AC30 are completed by this evidence, story
closure, exclusive local commit, and clean checkpoint.
