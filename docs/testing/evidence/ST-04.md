# ST-04 Validation Evidence

Date: 2026-08-02

Environment: Windows, Python 3.12 project virtual environment, offline runtime

Scope: Increment 4 — local knowledge base and deterministic lexical search

## Checkpoint

- Starting branch: `main`.
- Starting commit: `8dbf4d6f41b594b848dad7f53096ed546fe6f72b`.
- Starting version: `0.3.0`.
- Starting regression: 150 passed, exit 0.
- Starting worktree: clean; no Git remote configured.

## Functional evidence

- Exactly five packaged UTF-8 Markdown runbooks validate against the documented
  TOML metadata and canonical body structure.
- `knowledge search "sincronização OneDrive"` returns OneDrive at rank 1 with
  score 240, matched terms, revision, relative source, and excerpt.
- `knowledge search "conta bloqueada" --format json` returns the identity runbook
  at rank 1 with score 160 and structured evidence.
- An unmatched query returns `results: []` and exit 0.
- A punctuation-only query returns the safe validation contract and exit 2.
- The wheel and sdist version 0.4.0 include all five runbooks.

## Determinism and security evidence

- NFKD/casefold normalization proves case/accent equivalence while preserving
  display text.
- Field weights are title 50, aliases 40, symptoms 30, keywords 25, content 10;
  duplicate query terms count once per field.
- Total ordering is score descending, normalized title, document ID, and relative
  source path; repeated searches produce equivalent structured results.
- Missing, empty, malformed, duplicate-ID, invalid-UTF-8, structurally incomplete,
  duplicated/reordered-heading, symlink, and resolved escape corpora fail closed
  with generic safe errors and no partial results.
- IDs containing traversal, absolute paths, alternate separators, or drive paths
  are rejected. Runtime knowledge root is fixed to packaged content.
- Hostile content remains inert; scans found no shell, subprocess, network, LLM,
  dynamic evaluation, administrative executor, UI, or Increment 5 capability.

## Commands and exit codes

| Command | Result | Exit |
|---|---|---:|
| `.\\.venv\\Scripts\\python.exe -m ruff check .` | PASS | 0 |
| `.\\.venv\\Scripts\\python.exe -m mypy src tests` | PASS, strict | 0 |
| `.\\.venv\\Scripts\\python.exe -m pytest -q` | 192 passed, 1 skipped | 0 |
| `uv build --offline` | wheel and sdist 0.4.0 built | 0 |
| `git diff --check` | PASS | 0 |

The single skip is the symlink-escape runtime test when Windows does not grant
symlink creation. Containment was additionally covered by resolved-path tests,
adversarial review, and direct code inspection.

## Independent QA and security review

Initial QA found one High issue (body structure accepted incomplete Markdown) and
one Medium issue (empty corpus reported healthy). Development corrected both by
requiring exact ordered non-empty canonical sections, removing synthetic excerpt
fallback, and rejecting empty corpora. QA re-ran adversarial cases and all gates.

Final verdict: PASS. Acceptance criteria 1-20 satisfied after completion
administration. Zero open Critical, High, or Medium findings. CodeRabbit was not
configured; independent `@qa` review and automated gates were used.

No remote was configured, no push occurred, and Increment 5 was not started.
