# ST-05 Validation Evidence

Date: 2026-08-02

Environment: Windows, Python 3.12 project virtual environment, offline runtime

## Checkpoint and scope

- Started from clean `main` at `7be7b77917eaa6f3b2f3089b9446b91a24f91149`.
- Baseline version `0.4.0`; 192 passed and one conditional symlink skip.
- Increment 5 only: no Streamlit, Docker, Ollama, network, shell, or admin executor.

## Functional evidence

- Migration 3 persists ordered immutable performed procedures and documentation
  revisions with foreign keys, constraints, checksums, and rollback behavior.
- Suggested actions/approvals remain distinct from user-reported performed work.
- Documentation generation emits exactly nine ordered sections using only
  persisted incident-scoped facts and explicit neutral values for missing data.
- CLI journey passed for performed record/list, documentation generate/show/history,
  and both Markdown and JSON export from a real temporary SQLite database.
- Installed wheel reported version `0.5.0` and completed the same CLI journey.

## Export and security evidence

- Filenames are opaque and application-generated; callers provide no path/name.
- Root and all existing ancestors reject symlink/reparse escapes; resolved targets
  remain contained; collisions never overwrite.
- Publication is atomic and removes temporary files after write/flush/fsync/link
  failures.
- Markdown literalizes hostile headings, HTML, links, images, autolinks, fences,
  blockquotes, lists, control characters, and dangerous schemes while preserving
  the canonical nine headings.
- JSON is strict deterministic UTF-8 with stable schema and provenance.
- Corrupt persisted approval, triage, performed, or documentation data maps to
  typed safe errors without paths, SQL, traceback, secrets, or payload disclosure.
- No execution, subprocess, network, LLM, UI, or later-increment capability exists.

## Commands and results

| Command | Result | Exit |
|---|---|---:|
| `.\\.venv\\Scripts\\python.exe -m ruff check .` | PASS | 0 |
| `.\\.venv\\Scripts\\python.exe -m mypy src tests` | PASS, 53 files | 0 |
| `.\\.venv\\Scripts\\python.exe -m pytest -q` | 218 passed, 2 skipped | 0 |
| `uv build --offline` | wheel and sdist 0.5.0 | 0 |
| clean wheel install and CLI smoke | PASS | 0 |
| focused final remediation | 16 passed | 0 |
| `git diff --check` | PASS | 0 |

The two skips are environment-conditioned Windows symlink tests. Equivalent
containment paths were covered by adversarial input, resolved-path checks, and
independent code review.

## Independent QA

QA initially identified six Medium issues, then two residual Medium gaps. All
were corrected and re-reviewed. Final verdict: PASS, zero open Critical, High,
or Medium findings. AC1-AC24 passed technically; AC25 is satisfied by this
evidence, story closure, local exclusive commit, and clean post-commit checkpoint.

No remote was configured and no push occurred.
